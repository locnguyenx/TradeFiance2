'use client';

import React from 'react';
import { CheckersQueue } from '../../components/CheckersQueue';

export default function ApprovalsPage() {
  return (
    <div style={{ padding: '2rem' }}>
      <CheckersQueue />
    </div>
  );
}
